procedure dot_product(input tab1[], input tab2[])
    Var 
    Array tab1[], tab2[] 
    i:integer=0;
    j:integer=0;

for i=0 to length(tab1)-1 do
    tab1[i]=
end

for j=0 to length(tab2)-1 do
    tab2[j]=tab2[j]
end

for i=0 to length(tab1)-1 do
    c=c+tab1[i]*tab2[i]
end
       

    
end
